/* PROGRAMA ANTIGO ESTÁ DANDO FALHA
const puppeteer = require('puppeteer');

var pessoa = {};

console.log(pessoa);


async function robo() {
  const browser = await puppeteer.launch({headless: false}); //{headless: false} é oq permite ver oq está acontecendo
  const page = await browser.newPage();
  await page.goto('https://example.com');
  await page.screenshot({path: 'example.png'});

  await browser.close();
}

robo();
*/